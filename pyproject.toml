[project]
name = "notify-mcp"
version = "0.1.0"
description = "MCP Server for BaeumAI Notify API"
requires-python = ">=3.12"
dependencies = [
    "mcp[cli]>=1.0.0",
    "httpx>=0.28.0",
    "pydantic>=2.0.0",
]
